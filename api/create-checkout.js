module.exports=async(req,res)=>{if(req.method!=='POST'){return res.status(405).json({error: 'Method not allowed'});}try{const{email,service_type,amount,user_id}=req.body;const checkoutUrl=`https: return res.status(200).json({success: true,checkout_url: checkoutUrl,message: '支付链接已生成'});}catch(error){console.error('API Error:',error);return res.status(500).json({error: 'Internal server error'});}};